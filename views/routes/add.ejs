<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <title>Thêm Tuyến Đường</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/semantic-ui/dist/semantic.min.css"
    />
  </head>
  <body>
    <div class="ui container" style="margin-top: 20px">
      <h1>Thêm Tuyến Đường Mới</h1>
      <form class="ui form" action="/routes" method="post">
        <div class="field">
          <label>Tên Tuyến</label>
          <input type="text" name="route_name" placeholder="Tên Tuyến" />
        </div>
        <div class="field">
          <label>Nơi Khởi Hành</label>
          <input
            type="text"
            name="start_location"
            placeholder="Nơi Khởi Hành"
          />
        </div>
        <div class="field">
          <label>Nơi Đến</label>
          <input type="text" name="end_location" placeholder="Nơi Đến" />
        </div>
        <div class="field">
          <label>Khoảng Cách (km)</label>
          <input
            type="number"
            step="0.01"
            name="distance_km"
            placeholder="Khoảng Cách"
          />
        </div>
        <div class="field">
          <label>Thời Gian Dự Kiến (HH:MM:SS)</label>
          <input
            type="text"
            name="expected_duration"
            placeholder="Thời Gian Dự Kiến"
          />
        </div>
        <div class="field">
          <label>Giá Vé</label>
          <input
            type="number"
            step="0.01"
            name="ticket_price"
            placeholder="Giá Vé"
          />
        </div>
        <button type="submit" class="ui primary button">Thêm</button>
      </form>
    </div>
  </body>
</html>
