<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <title>Chỉnh Sửa Điểm Dừng</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/semantic-ui/dist/semantic.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h1>Chỉnh Sửa Điểm Dừng</h1>
      <form
        action="/route-stops/<%= stop.id %>?_method=PUT"
        method="post"
        class="ui form"
      >
        <div class="field">
          <label>Route ID</label>
          <input
            type="number"
            name="route_id"
            value="<%= stop.route_id %>"
            required
          />
        </div>
        <div class="field">
          <label>Tên Điểm Dừng</label>
          <input
            type="text"
            name="stop_name"
            value="<%= stop.stop_name %>"
            required
          />
        </div>
        <div class="field">
          <label>Thứ Tự</label>
          <input
            type="number"
            name="stop_order"
            value="<%= stop.stop_order %>"
            required
          />
        </div>
        <div class="field">
          <label>Giờ Đến Dự Kiến</label>
          <input
            type="time"
            name="estimated_arrival_time"
            value="<%= stop.estimated_arrival_time %>"
            required
          />
        </div>
        <button type="submit" class="ui orange button">Cập Nhật</button>
        <a href="/route-stops" class="ui button">Quay Lại</a>
      </form>
    </div>
  </body>
</html>
