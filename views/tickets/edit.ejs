<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <title>Chỉnh Sửa Vé</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/semantic-ui/dist/semantic.min.css"
    />
  </head>
  <body>
    <div class="ui container" style="margin-top: 20px">
      <h1>Chỉnh Sửa Vé</h1>
      <form
        class="ui form"
        action="/tickets/<%= ticket.id %>?_method=PUT"
        method="post"
      >
        <div class="field">
          <label>Lịch Trình</label>
          <input
            type="number"
            name="schedule_id"
            value="<%= ticket.schedule_id %>"
            placeholder="Lịch Trình"
          />
        </div>
        <div class="field">
          <label>Số Ghế</label>
          <input
            type="text"
            name="seat_number"
            value="<%= ticket.seat_number %>"
            placeholder="Số Ghế"
          />
        </div>
        <div class="field">
          <label>Giá</label>
          <input
            type="number"
            step="0.01"
            name="price"
            value="<%= ticket.price %>"
            placeholder="Giá"
          />
        </div>
        <div class="field">
          <label>Trạng Thái</label>
          <select name="status" class="ui dropdown">
            <option value="Đã đặt" <%= ticket.status === 'Đã đặt' ? 'selected' : '' %>>Đã đặt</option>
            <option value="Đã thanh toán" <%= ticket.status === 'Đã thanh toán' ? 'selected' : '' %>>Đã thanh toán</option>
            <option value="Đã sử dụng" <%= ticket.status === 'Đã sử dụng' ? 'selected' : '' %>>Đã sử dụng</option>
            <option value="Hủy" <%= ticket.status === 'Hủy' ? 'selected' : '' %>>Hủy</option>
          </select>
        </div>
        <div class="field">
          <label>Mã QR</label>
          <input
            type="text"
            name="qr_code"
            value="<%= ticket.qr_code %>"
            placeholder="Mã QR"
          />
        </div>
        <div class="field">
          <label>Tên Hành Khách</label>
          <input
            type="text"
            name="passenger_name"
            value="<%= ticket.passenger_name %>"
            placeholder="Tên Hành Khách"
          />
        </div>
        <div class="field">
          <label>Số Điện Thoại</label>
          <input
            type="text"
            name="passenger_phone"
            value="<%= ticket.passenger_phone %>"
            placeholder="Số Điện Thoại"
          />
        </div>
        <div class="field">
          <label>Số CMND/CCCD</label>
          <input
            type="text"
            name="passenger_id_number"
            value="<%= ticket.passenger_id_number %>"
            placeholder="Số CMND/CCCD"
          />
        </div>
        <div class="field">
          <label>Hạng Ghế</label>
          <select name="seat_class" class="ui dropdown">
            <option value="Thường" <%= ticket.seat_class === 'Thường' ? 'selected' : '' %>>Thường</option>
            <option value="VIP" <%= ticket.seat_class === 'VIP' ? 'selected' : '' %>>VIP</option>
            <option value="Deluxe" <%= ticket.seat_class === 'Deluxe' ? 'selected' : '' %>>Deluxe</option>
          </select>
        </div>
        <button type="submit" class="ui primary button">Cập nhật</button>
        <button type="button" class="ui button" onclick="window.history.back();">Hủy</button>
      </form>
    </div>
  </body>
</html>
