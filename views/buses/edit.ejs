<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chỉnh Sửa Thông Tin Xe Bus</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/semantic-ui/dist/semantic.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h1>Chỉnh Sửa Thông Tin Xe Bus</h1>
      <form action="/buses/<%= bus.id %>?_method=PUT" method="post" enctype="multipart/form-data">
        <input type="hidden" name="_method" value="PUT" />
        
        <div class="mb-3">
          <label for="license_plate" class="form-label">Biển Số Xe</label>
          <input
            type="text"
            id="license_plate"
            name="license_plate"
            class="form-control"
            value="<%= bus.license_plate %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="bus_type" class="form-label">Loại Xe</label>
          <select id="bus_type" name="bus_type" class="form-select" required>
            <option value="Ghế ngồi" <%= bus.bus_type === 'Ghế ngồi' ? 'selected' : '' %>>Ghế ngồi</option>
            <option value="Giường nằm" <%= bus.bus_type === 'Giường nằm' ? 'selected' : '' %>>Giường nằm</option>
            <option value="Limousine" <%= bus.bus_type === 'Limousine' ? 'selected' : '' %>>Limousine</option>
          </select>
        </div>

        <div class="mb-3">
          <label for="capacity" class="form-label">Sức Chứa</label>
          <input
            type="number"
            id="capacity"
            name="capacity"
            class="form-control"
            value="<%= bus.capacity %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="brand" class="form-label">Thương Hiệu</label>
          <input
            type="text"
            id="brand"
            name="brand"
            class="form-control"
            value="<%= bus.brand %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="manufacture_year" class="form-label">Năm Sản Xuất</label>
          <input
            type="number"
            id="manufacture_year"
            name="manufacture_year"
            class="form-control"
            value="<%= bus.manufacture_year %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="status" class="form-label">Trạng Thái</label>
          <select id="status" name="status" class="form-select" required>
            <option value="Hoạt động" <%= bus.status === 'Hoạt động' ? 'selected' : '' %>>Hoạt động</option>
            <option value="Bảo trì" <%= bus.status === 'Bảo trì' ? 'selected' : '' %>>Bảo trì</option>
            <option value="Ngừng sử dụng" <%= bus.status === 'Ngừng sử dụng' ? 'selected' : '' %>>Ngừng sử dụng</option>
          </select>
        </div>

        <div class="mb-3">
          <label for="driver_id" class="form-label">Mã Lái Xe</label>
          <input
            type="number"
            id="driver_id"
            name="driver_id"
            class="form-control"
            value="<%= bus.driver_id %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="image" class="form-label">Hình Ảnh</label>
          <input type="file" id="image" name="image" class="form-control" />
          <div class="mt-2">
            <% if (bus.image) { %>
              <img src="<%= bus.image %>" alt="Hình ảnh hiện tại" style="max-width: 100px;" />
            <% } else { %>
              <p>Không có hình ảnh hiện tại.</p>
            <% } %>
          </div>
        </div>

        <button type="submit" class="btn btn-primary">Cập Nhật Xe Bus</button>
        <a href="/buses" class="btn btn-secondary">Quay lại danh sách</a>
      </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/semantic-ui/dist/semantic.min.js"></script>
  </body>
</html>
